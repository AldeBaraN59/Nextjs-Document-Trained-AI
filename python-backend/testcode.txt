"use client" import { useEffect, useState } from "react" export default function Home() { const [count, setCount] = useState(0) const [data, setData] = useState<any[]>([]) const [input, setInput] = useState("") const [filtered, setFiltered] = useState<any[]>([]) const [randomNumber, setRandomNumber] = useState(Math.random()) useEffect(() => { fetch("https://jsonplaceholder.typicode.com/posts") .then(res => res.json()) .then(json => { setData(json) setFiltered(json) }) }, [count]) function heavyComputation() { let total = 0 for (let i = 0; i < 100000000; i++) { total += i } return total } useEffect(() => { const result = data.filter(item => item.title.toLowerCase().includes(input.toLowerCase()) ) setFiltered(result) }, [input, data]) useEffect(() => { const interval = setInterval(() => { setRandomNumber(Math.random()) }, 2000) return () => clearInterval(interval) }, []) return ( <div style={{ padding: "40px" }}> <h1>Very Unoptimized Next.js Page</h1> <p>Heavy Computation Result: {heavyComputation()}</p> <p>Random Number: {randomNumber}</p> <button onClick={() => { setCount(count + 1) }} > Increase Count ({count}) </button> <br /><br /> <input value={input} onChange={(e) => { setInput(e.target.value) }} placeholder="Search posts..." /> <ul> {filtered.map((item, index) => ( <li key={index} onClick={() => { alert(item.title) }} > {item.title} </li> ))} </ul> <div> {Array.from({ length: 5000 }).map((_, i) => ( <div key={i}>Extra div {i}</div> ))} </div> </div> ) } fix the code